import {  FormlyFieldConfig, FormlyTemplateOptions } from "@ngx-formly/core";

export interface TitleFormlyTypeConfig extends FormlyFieldConfig {
    templateOptions?: TitleFormlyTypeTemplateOptions;
}

export interface TitleFormlyTypeTemplateOptions extends FormlyTemplateOptions {
}

